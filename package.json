{
  "name": "motiv",
  "version": "0.1.0",
  "description": "Autonomous development agent that ingests work items and implements code changes",
  "type": "module",
  "bin": {
    "motiv": "./src/index.js"
  },
  "scripts": {
    "start": "bun run src/index.js",
    "test": "bun test",
    "build": "bun build --compile src/index.js --outfile motiv"
  }
}
